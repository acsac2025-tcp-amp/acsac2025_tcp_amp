1. Run the tagging script on the full ISN dataset collected during the initial scanning (/scan folder) to tag hosts with their corresponding predictable ISN pattern types

```
python3 tagging.py ISN_set_filterd.csv tagging_result.csv
```